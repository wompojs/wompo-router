{
  "name": "womp-router",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild src/App.tsx --outdir=dist --bundle --watch --allow-overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "womp": "../womp"
  },
  "devDependencies": {
		"esbuild": "0.20.1",
		"ts-loader": "^9.4.3"
	}
}
